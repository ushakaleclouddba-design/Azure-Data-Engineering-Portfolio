# Sample outputs

This folder is reserved for sanitized sample report outputs. To be added in a future commit:

- `sample_assessment_report.xlsx` — A redacted sample run against a synthetic 3-server estate
- Screenshots of:
  - Executive Summary tab
  - Methodology & Sources tab
  - A per-server tab showing the AI recommendation block

If you'd like to see actual output, clone the repo, set up an `ANTHROPIC_API_KEY`, and run against any reachable SQL Server. Even a single-server local instance produces a complete report in under a minute.
